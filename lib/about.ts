/**
 * AboutContent singleton (id=1). Domain katmanindan re-export — mevcut
 * `@/lib/about` importlari korunur. Sorgu React cache() ile sarilidir.
 */
export { getAboutContent } from "@/lib/data/queries";
